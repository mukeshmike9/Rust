{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(OSX) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/guessing_game/target/debug/guessing_game",
            "args": [],
            "cwd": "${workspaceRoot}",
        }
    ]
}