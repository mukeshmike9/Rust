{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(OSX) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/001.HelloWorld/hello",
            "args": [],
            "cwd": "${workspaceRoot}",
        }
    ]
}